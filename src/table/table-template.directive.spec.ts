import { TableTemplateDirective } from './table-template.directive';

describe('TableTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new TableTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
